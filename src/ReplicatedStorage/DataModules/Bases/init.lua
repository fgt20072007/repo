

return {
	[1] = {
		BaseName = "Six Seven 67's Base", -- Name of the base
		BasePrice = 0, -- Price of the base
		LuckAmount = 1, -- Amount of luck (only for display reasons)
		Percentages = {
			Common = 100,
		}, -- Percentages of rarities being found
		Speed = 25, -- Speed of the chaser
		BaseDefender = script:WaitForChild("SixSeven"), -- Model of the chaser
		Orientation = 360,
		RobuxPurchasables = { -- Purchasable list of purchasable entities with robux
			[1] = {
				PurchaseId = 0,
				EntityName = "Default"
			}
		},
		
		WalkingAnimation = 0, -- Walking animation for the follower
		IdleAnimation = 0, -- Idle animation for the follower (Replace these to what you want)
	},
	
	
	[2] = {
		BaseName = "Tralalero Tralala's Base", -- Name of the base
		BasePrice = 0, -- Price of the base
		LuckAmount = 1, -- Amount of luck (only for display reasons)
		Percentages = {
			Common = 100,
		}, -- Percentages of rarities being found
		Speed = 25, -- Speed of the chaser
		BaseDefender = script:WaitForChild("Tralalero Tralala"), -- Model of the chaser
		Orientation = 360,
		RobuxPurchasables = { -- Purchasable list of purchasable entities with robux
			[1] = {
				PurchaseId = 0,
				EntityName = "Default"
			}
		},

		WalkingAnimation = 0, -- Walking animation for the follower
		IdleAnimation = 0, -- Idle animation for the follower (Replace these to what you want)
	},
	
	[3] = {
		BaseName = "La Vacca Saturno Saturnita's Base", -- Name of the base
		BasePrice = 0, -- Price of the base
		LuckAmount = 1, -- Amount of luck (only for display reasons)
		Percentages = {
			Common = 100,
		}, -- Percentages of rarities being found
		Speed = 25, -- Speed of the chaser
		BaseDefender = script:WaitForChild("La Vacca Saturno Saturnita"), -- Model of the chaser
		Orientation = 360,
		RobuxPurchasables = { -- Purchasable list of purchasable entities with robux
			[1] = {
				PurchaseId = 0,
				EntityName = "Default"
			}
		},

		WalkingAnimation = 0, -- Walking animation for the follower
		IdleAnimation = 0, -- Idle animation for the follower (Replace these to what you want)
	},
	
	[4] = {
		BaseName = "Chillin Chilli's Base", -- Name of the base
		BasePrice = 0, -- Price of the base
		LuckAmount = 1, -- Amount of luck (only for display reasons)
		Percentages = {
			Common = 100,
		}, -- Percentages of rarities being found
		Speed = 25, -- Speed of the chaser
		BaseDefender = script:WaitForChild("ChillinChilli"), -- Model of the chaser
		Orientation = 180,
		RobuxPurchasables = { -- Purchasable list of purchasable entities with robux
			[1] = {
				PurchaseId = 0,
				EntityName = "Default"
			}
		},

		WalkingAnimation = 0, -- Walking animation for the follower
		IdleAnimation = 0, -- Idle animation for the follower (Replace these to what you want)
	},
	
	[5] = {
		BaseName = "Strawberry Elephant's Base", -- Name of the base
		BasePrice = 0, -- Price of the base
		LuckAmount = 1, -- Amount of luck (only for display reasons)
		Percentages = {
			Common = 100,
		}, -- Percentages of rarities being found
		Speed = 25, -- Speed of the chaser
		BaseDefender = script:WaitForChild("Template"), -- Model of the chaser
		Orientation = 270,
		RobuxPurchasables = { -- Purchasable list of purchasable entities with robux
			[1] = {
				PurchaseId = 0,
				EntityName = "Default"
			}
		},

		WalkingAnimation = 0, -- Walking animation for the follower
		IdleAnimation = 0, -- Idle animation for the follower (Replace these to what you want)
	},
	
	[6] = {
		BaseName = "John Pork's Base", -- Name of the base
		BasePrice = 0, -- Price of the base
		LuckAmount = 1, -- Amount of luck (only for display reasons)
		Percentages = {
			Common = 100,
		}, -- Percentages of rarities being found
		Speed = 25, -- Speed of the chaser
		BaseDefender = script:WaitForChild("JohnPork"), -- Model of the chaser
		Orientation = 270,
		RobuxPurchasables = { -- Purchasable list of purchasable entities with robux
			[1] = {
				PurchaseId = 0,
				EntityName = "Default"
			}
		},

		WalkingAnimation = 0, -- Walking animation for the follower
		IdleAnimation = 0, -- Idle animation for the follower (Replace these to what you want)
	},
}